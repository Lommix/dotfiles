### BASH TOOL RULES:

ALWAYS use `rg` instead of `grep` or `find`!
ALWAYS work with a clear limit when using `rg`!

### Docs and Refs

Zig std `/usr/lib/zig/std` You are encouraged to search it to solve a problem
